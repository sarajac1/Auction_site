import { BrowserRouter, Routes, Route } from "react-router-dom"

export default function Router() {
  return (
    <BrowserRouter>
      <NavBar />
      <Routes>
        <Route />
      </Routes>
    </BrowserRouter>
  )
}